<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_equipment_repairTask_detail:设备维修任务明细表 Mapper.xml
创建人：陈刚 自动创建
创建时间：2019-07-01
 -->
<mapper namespace="com.xy.vmes.deecoop.equipment.dao.EquipmentRepairTaskDetailMapper">

    <!-- 批量删除 自动创建，禁止修改-->
    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_equipment_repairTask_detail
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
		#{item}
        </foreach>
    </delete>

    <!-- 自动创建，禁止修改-->
    <update id="updateToDisableByIds" parameterType="java.lang.String" >
        update vmes_equipment_repairTask_detail
        set isdisable = '0' ,udate = now()
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
        #{item}
        </foreach>
    </update>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->
    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'equipmentRepairTaskDetail' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','taskId','维修任务ID',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','productId','备件ID(货品)ID',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','receiveCount','领取数量',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','receiveOutdtlId','领取出库明细ID',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','applyCount','实际使用数量',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','retreatType','退回方式(1:退回备件库, 2:退回虚拟库)',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','retreatCount','退回数量',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','retreatIndtlId','退回入库明细ID',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','warehouseId','虚拟库ID(货位ID)',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','cuser','创建用户id',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','cdate','创建时间',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','uuser','修改用户id',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','udate','修改时间',13, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','remark','备注',14, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','isdisable','是否启用(0:已禁用 1:启用)',15, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'equipmentRepairTaskDetail','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>


    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.EquipmentRepairTaskDetail">
        select
        id id,
        task_id taskId,
        product_id productId,
        receive_count receiveCount,
        receive_outdtl_id receiveOutdtlId,

        apply_count applyCount,
        retreat_type retreatType,
        retreat_count retreatCount,
        retreat_indtl_id retreatIndtlId,
        warehouse_id warehouseId,

        cuser cuser,
        cdate cdate,
        uuser uuser,
        udate udate,
        remark remark,

        isdisable isdisable
        from
        <include refid="tableName"></include>
        <where>
            <!--isSelfExist 是否考虑自己在业务表中是否存在
                false: (false or is null) 无需考虑自己在业务表中是否存在
                true : 需要考虑自己在业务表中是否存在
            -->
            <if test="id != null and id != ''" >
                <choose>
                    <when test="'true' == isSelfExist">
                        <![CDATA[ and id <> #{id} ]]>
                    </when>
                    <otherwise>
                        and id = #{id}
                    </otherwise>
                </choose>
            </if>
            <if test="taskId != null and taskId != ''">
                and task_id = #{taskId}
            </if>
            <if test="productId != null and productId != ''">
                and product_id = #{productId}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>

    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
        id id,
        task_id taskId,
        product_id productId,
        receive_count receiveCount,
        receive_outdtl_id receiveOutdtlId,

        apply_count applyCount,
        retreat_type retreatType,
        retreat_count retreatCount,
        retreat_indtl_id retreatIndtlId,
        warehouse_id warehouseId,

        cuser cuser,
        cdate cdate,
        uuser uuser,
        udate udate,
        remark remark,

        isdisable isdisable
        from vmes_equipment_repairTask_detail
        <where>
            <if test="taskId != null and taskId != ''">
                and task_id = #{taskId}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>