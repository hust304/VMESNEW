<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_loginfo:系统日志表 Mapper.xml
创建人：陈刚 自动创建
创建时间：2018-08-08
 -->
<mapper namespace="com.xy.vmes.deecoop.system.dao.LoginfoMapper">

    <!--表名 自动创建，禁止修改-->
    <sql id="tableName">
	vmes_loginfo
    </sql>

    <!-- 字段 自动创建，禁止修改-->
    <sql id="Field">
		type type,<!-- 日志类型 operate:操作日志 state:状态日志 -->
		business_id businessId,<!-- 业务单ID -->
		business_type businessType,<!-- 业务类别(1:生产计划 2:产品 3:部门 4:部门子计划 5:派工单 6:子派工单) -->
		source source,<!-- 产生源(app:终端操作 web:web端操作 sys:系统产生) -->
		table_name tableName,<!-- 业务名称 (表名) -->
		operate operate,<!-- 业务操作类型 (add,update,delete) -->
		remark remark,<!-- 备注 -->
		cdate cdate,<!-- 创建时间 -->
		cuser cuser,<!-- 创建人账号 -->
		operate_value operateValue,<!-- (删除,修改)-业务id字符串 -->
		id id <!-- id -->
    </sql>

    <!-- 字段值 自动创建，禁止修改-->
    <sql id="FieldValue">
		#{type},<!-- 日志类型 operate:操作日志 state:状态日志 -->
		#{businessId},<!-- 业务单ID -->
		#{businessType},<!-- 业务类别(1:生产计划 2:产品 3:部门 4:部门子计划 5:派工单 6:子派工单) -->
		#{source},<!-- 产生源(app:终端操作 web:web端操作 sys:系统产生) -->
		#{tableName},<!-- 业务名称 (表名) -->
		#{operate},<!-- 业务操作类型 (add,update,delete) -->
		#{remark},<!-- 备注 -->
		#{cdate},<!-- 创建时间 -->
		#{cuser},<!-- 创建人账号 -->
		#{operateValue},<!-- (删除,修改)-业务id字符串 -->
		#{id} <!-- id -->
    </sql>


    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.Loginfo">
        select
            <include refid="Field"></include>
        from
            <include refid="tableName"></include>
        <choose>
            <!--
                mapSize (0 or is null) 无查询参数-业务层传入
                isQueryAll 是否查询全部
                fasle: (false or is null) 无查询条件-查询结果集返回空或list.size()==0
                true : 无查询条件-返回全部业务表数据
            -->
            <when test="(mapSize == null || mapSize == 0) and 'true' != isQueryAll ">
                where 1=2
            </when>
            <otherwise>
                <where>
                    <!--isSelfExist 是否考虑自己在业务表中是否存在
                        false: (false or is null) 无需考虑自己在业务表中是否存在
                        true : 需要考虑自己在业务表中是否存在
                    -->
                    <if test="id != null and id != ''" >
                        <choose>
                            <when test="'true' == isSelfExist">
                                <![CDATA[ and id <> #{id} ]]>
                            </when>
                            <otherwise>
                                and id = #{id}
                            </otherwise>
                        </choose>
                    </if>


                    <!--queryStr 自定义sql查询语句-->
                    <if test="queryStr != null and queryStr != ''" >
                        and ${queryStr}
                    </if>
                </where>
            </otherwise>
        </choose>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>



    <!-- 系统日志分页查询List-->
    <sql id="Column1">
            '日志类型 operate:操作日志 state:状态日志'  type,
            '业务单ID'  businessId,
            '业务类别(1:生产计划 2:产品 3:部门 4:部门子计划 5:派工单 6:子派工单)'  businessType,
            '产生源(app:终端操作 web:web端操作 sys:系统产生)'  source,
            '业务名称 (表名)'  tableName,
            '业务操作类型 (add,update,delete)'  operate,
            '备注'  remark,
            '创建时间'  cdate,
            '创建人账号'  cuser,
            '(删除,修改)-业务id字符串'  operateValue,
            'id' id
    </sql>
    <select id="getColumnList"  resultType="java.util.LinkedHashMap">
        select
        <include refid="Column1"></include>
        from dual
    </select>


    <select id="getDataListPage"  parameterType="com.yvan.PageData"  resultType="java.util.Map">
        select
        <include refid="Field"></include>
        from
        <include refid="tableName"></include>
        <where>
            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
            <if test="keywords!= null and keywords != ''"><!-- 关键词检索 -->
                and
                (
                <!--	根据需求自己加检索条件
                            字段1 LIKE CONCAT(CONCAT('%', #{keywords}),'%')
                             or
                            字段2 LIKE CONCAT(CONCAT('%', #{keywords}),'%')
                        -->
                )
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>