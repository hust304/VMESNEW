<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_sale_invoice_detail:发票明细 Mapper.xml
创建人：陈刚 自动创建
创建时间：2019-01-08
 -->
<mapper namespace="com.xy.vmes.deecoop.sale.dao.SaleInvoiceDetailMapper">
    <!-- 批量删除 自动创建，禁止修改-->
    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_sale_invoice_detail
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
		#{item}
        </foreach>
    </delete>

    <!-- 自动创建，禁止修改-->
    <update id="updateToDisableByIds" parameterType="java.lang.String" >
        update vmes_sale_invoice_detail
        set isdisable = '0' ,udate = now()
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
        #{item}
        </foreach>
    </update>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->
    <update id="updateStateByDetail" parameterType="com.yvan.PageData" >
        update vmes_sale_invoice_detail set
        state = #{state},
        udate = now()
        <where>
            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
            <if test="parentIds != null and parentIds != ''" >
                and ${parentIds}
            </if>
        </where>
    </update>

    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'saleInvoiceDetail' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'saleInvoiceDetail','parentId','发票ID',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','orderId','订单ID',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','orderDetaiId','订单明细ID',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','state','发票状态(0:待开票 1:已开票 -1:已开票)',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','count','开票数量',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','productPrice','货品单价',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','sum','发票金额',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','cuser','创建用户id',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','cdate','创建时间',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','uuser','修改用户id',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','udate','修改时间',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','remark','备注',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','isdisable','是否启用(0:已禁用 1:启用)',13, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoiceDetail','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>


    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.SaleInvoiceDetail">
        select
        id id,
        parent_id parentId,
        order_id orderId,
        order_detai_id orderDetaiId,
        state state,

        count count,
        product_price productPrice,
        sum sum,
        cuser cuser,
        cdate cdate,

        uuser uuser,
        udate udate,
        remark remark,
        isdisable isdisable
        from vmes_sale_invoice_detail
        <where>
            <!--isSelfExist 是否考虑自己在业务表中是否存在
                false: (false or is null) 无需考虑自己在业务表中是否存在
                true : 需要考虑自己在业务表中是否存在
            -->
            <if test="id != null and id != ''" >
                <choose>
                    <when test="'true' == isSelfExist">
                        <![CDATA[ and id <> #{id} ]]>
                    </when>
                    <otherwise>
                        and id = #{id}
                    </otherwise>
                </choose>
            </if>
            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
            <if test="state != null and state != ''" >
                and state = #{state}
            </if>


            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
        id id,
        parent_id parentId,
        order_id orderId,
        order_detai_id orderDetaiId,
        state state,

        count count,
        product_price productPrice,
        sum sum,
        cuser cuser,
        cdate cdate,

        uuser uuser,
        udate udate,
        remark remark,
        isdisable isdisable
        from vmes_sale_invoice_detail
        <where>
            <if test="ids != null and ids != ''" >
                and id in (${ids})
            </if>
            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
            <if test="state != null and state != ''" >
                and state = #{state}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>