<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_sale_invoice:发票 Mapper.xml
创建人：陈刚 自动创建
创建时间：2019-01-08
 -->
<mapper namespace="com.xy.vmes.deecoop.sale.dao.SaleInvoiceMapper">

    <!-- 批量删除 自动创建，禁止修改-->
    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_sale_invoice
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>

    <!-- 自动创建，禁止修改-->
    <update id="updateToDisableByIds" parameterType="java.lang.String" >
        update vmes_sale_invoice
        set isdisable = '0' ,udate = now()
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
            #{item}
        </foreach>
    </update>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->

    <update id="updateStateByInvoice" parameterType="com.yvan.PageData" >
        update vmes_sale_invoice set
        state = #{state},
        udate = now()
        <where>
            <if test="ids != null and ids != ''" >
                and ${ids}
            </if>
        </where>
    </update>

    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'saleInvoice' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'saleInvoice','companyId','企业ID',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','customerId','客户ID',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','orderId','订单ID',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','sysCode','发票编号(系统生成)',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','invoiceNumber','发票编号(用户)',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','state','发票状态(0:待开票 1:已开票 -1:已开票)',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','cuser','创建用户id',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','cdate','创建时间',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','uuser','修改用户id',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','udate','修改时间',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','remark','备注',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','isdisable','是否启用(0:已禁用 1:启用)',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleInvoice','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>


    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.SaleInvoice">
        select
        id id,
        company_id companyId,
        customer_id customerId,
        order_id orderId,
        sys_code sysCode,

        invoice_number invoiceNumber,
        state state,
        cuser cuser,
        cdate cdate,
        uuser uuser,

        udate udate,
        remark remark,
        isdisable isdisable
        from vmes_sale_invoice
        <where>
            <!--isSelfExist 是否考虑自己在业务表中是否存在
                false: (false or is null) 无需考虑自己在业务表中是否存在
                true : 需要考虑自己在业务表中是否存在
            -->
            <if test="id != null and id != ''" >
                <choose>
                    <when test="'true' == isSelfExist">
                        <![CDATA[ and id <> #{id} ]]>
                    </when>
                    <otherwise>
                        and id = #{id}
                    </otherwise>
                </choose>
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
        id id,
        company_id companyId,
        customer_id customerId,
        order_id orderId,
        sys_code sysCode,

        invoice_number invoiceNumber,
        state state,
        cuser cuser,
        cdate cdate,
        uuser uuser,

        udate udate,
        remark remark,
        isdisable isdisable
        from vmes_sale_invoice
        <where>
            <if test="currentCompanyId != null and currentCompanyId != ''" >
                and company_id = #{currentCompanyId}
            </if>
            <if test="sysCode != null and sysCode != ''" >
                and sys_code = #{sysCode}
            </if>
            <if test="invoiceNumber != null and invoiceNumber != ''" >
                and invoice_number = #{orderNumber}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>

        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>