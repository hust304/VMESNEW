<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_saleOrde_change:订单变更记录表 Mapper.xml
创建人：陈刚 自动创建
创建时间：2019-11-05
 -->
<mapper namespace="com.xy.vmes.deecoop.sale.dao.SaleOrdeChangeMapper">

    <!-- 批量删除 自动创建，禁止修改-->
    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_saleOrde_change
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
		#{item}
        </foreach>
    </delete>

    <!-- 自动创建，禁止修改-->
    <update id="updateToDisableByIds" parameterType="java.lang.String" >
        update vmes_saleOrde_change
        set isdisable = '0' ,udate = now()
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
        #{item}
        </foreach>
    </update>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->
    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'saleOrdeChange' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'saleOrdeChange','companyId','企业id',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','orderId','订单id',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','auditId','审核人id',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','state','状态(0:审核中 1:完成:审核通过 2:取消:审核不通过)',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','receiptTypeBefore','发票类型(变更前-1:不开票 2:普票发票 3:增值税发票)',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','receiptTypeAfter','发票类型(变更后-1:不开票 2:普票发票 3:增值税发票)',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','cuser','创建用户id',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','cdate','创建时间',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','uuser','修改用户id',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','udate','修改时间',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','remark','备注',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','isdisable','是否启用(0:已禁用 1:启用)',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeChange','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>

    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.SaleOrdeChange">
        select
            id id,
            company_id companyId,
            order_id orderId,
            audit_id auditId,
            state state,

            receipt_type_before receiptTypeBefore,
            receipt_type_after receiptTypeAfter,
            cuser cuser,
            cdate cdate,
            uuser uuser,
            udate udate,

            remark remark,
            isdisable isdisable
        from vmes_saleOrde_change
        <where>
            <!--isSelfExist 是否考虑自己在业务表中是否存在
                false: (false or is null) 无需考虑自己在业务表中是否存在
                true : 需要考虑自己在业务表中是否存在
            -->
            <if test="id != null and id != ''" >
                <choose>
                    <when test="'true' == isSelfExist">
                        <![CDATA[ and id <> #{id} ]]>
                    </when>
                    <otherwise>
                        and id = #{id}
                    </otherwise>
                </choose>
            </if>

            <if test="companyId != null and companyId != ''" >
                and company_id = #{companyId}
            </if>
            <if test="orderId != null and orderId != ''" >
                and order_id = #{orderId}
            </if>
            <if test="state != null and state != ''" >
                and state = #{state}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
        id id,
        company_id companyId,
        order_id orderId,
        audit_id auditId,
        state state,

        receipt_type_before receiptTypeBefore,
        receipt_type_after receiptTypeAfter,
        cuser cuser,
        cdate cdate,
        uuser uuser,
        udate udate,

        remark remark,
        isdisable isdisable
        from vmes_saleOrde_change
        <where>
            <if test="companyId != null and companyId != ''" >
                and company_id = #{companyId}
            </if>
            <if test="orderId != null and orderId != ''" >
                and order_id = #{orderId}
            </if>
            <if test="state != null and state != ''" >
                and state = #{state}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>

        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>