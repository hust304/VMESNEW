<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_saleOrdeDtl_change:订单明细变更记录表 Mapper.xml
创建人：陈刚 自动创建
创建时间：2019-11-05
 -->
<mapper namespace="com.xy.vmes.deecoop.sale.dao.SaleOrdeDtlChangeMapper">

    <!-- 批量删除 自动创建，禁止修改-->
    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_saleOrdeDtl_change
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
		#{item}
        </foreach>
    </delete>

    <!-- 自动创建，禁止修改-->
    <update id="updateToDisableByIds" parameterType="java.lang.String" >
        update vmes_saleOrdeDtl_change
        set isdisable = '0' ,udate = now()
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
        #{item}
        </foreach>
    </update>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->
    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'saleOrdeDtlChange' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','parentId','订单变更id',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','orderDtlId','订单明细id',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','orderCountBefore','订单订购数量(变更前-订单单位)',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','orderCountAfter','订单订购数量(变更后-订单单位)',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','productPriceBefore','货品单价(变更前)',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','productPriceAfter','货品单价(变更后)',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','deliverDateBefore','约定交期(变更前)',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','deliverDateAfter','约定交期(变更后)',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','deliverCount','发货单数量(订单单位)',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','endDeliverCount','完成发货数量(订单单位)',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','cuser','创建用户id',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','cdate','创建时间',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','uuser','修改用户id',13, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','udate','修改时间',14, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','remark','备注',15, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','isdisable','是否启用(0:已禁用 1:启用)',16, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'saleOrdeDtlChange','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>

    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.SaleOrdeDtlChange">
        select
            id id,
            parent_id parentId,
            order_dtl_id orderDtlId,
            order_count_before orderCountBefore,
            order_count_after orderCountAfter,

            product_price_before productPriceBefore,
            product_price_after productPriceAfter,
            deliver_date_before deliverDateBefore,
            deliver_date_after deliverDateAfter,
            deliver_count deliverCount,

            end_deliver_count endDeliverCount,
            cuser cuser,
            cdate cdate,
            uuser uuser,
            udate udate,

            remark remark,
            isdisable isdisable
        from vmes_saleOrdeDtl_change
        <where>
            <!--isSelfExist 是否考虑自己在业务表中是否存在
                false: (false or is null) 无需考虑自己在业务表中是否存在
                true : 需要考虑自己在业务表中是否存在
            -->
            <if test="id != null and id != ''" >
                <choose>
                    <when test="'true' == isSelfExist">
                        <![CDATA[ and id <> #{id} ]]>
                    </when>
                    <otherwise>
                        and id = #{id}
                    </otherwise>
                </choose>
            </if>

            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
            <if test="orderDtlId != null and orderDtlId != ''" >
                and order_dtl_id = #{orderDtlId}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>

    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
        id id,
        parent_id parentId,
        order_dtl_id orderDtlId,
        order_count_before orderCountBefore,
        order_count_after orderCountAfter,

        product_price_before productPriceBefore,
        product_price_after productPriceAfter,
        deliver_date_before deliverDateBefore,
        deliver_date_after deliverDateAfter,
        deliver_count deliverCount,

        end_deliver_count endDeliverCount,
        cuser cuser,
        cdate cdate,
        uuser uuser,
        udate udate,

        remark remark,
        isdisable isdisable
        from vmes_saleOrdeDtl_change
        <where>
            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
            <if test="orderDtlId != null and orderDtlId != ''" >
                and order_dtl_id = #{orderDtlId}
            </if>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>


</mapper>