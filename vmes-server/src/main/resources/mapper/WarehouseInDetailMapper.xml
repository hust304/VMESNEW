<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
说明：vmes_warehouse_in_detail:仓库入库单明细 Mapper.xml
创建人：陈刚 自动创建
创建时间：2018-10-16
 -->
<mapper namespace="com.xy.vmes.deecoop.warehouse.dao.WarehouseInDetailMapper">

    <delete id="deleteByIds" parameterType="java.lang.String">
        delete from vmes_warehouse_in_detail
        where id in
        <foreach item="item" index="index" collection="array" open="(" separator="," close=")">
		#{item}
        </foreach>
    </delete>

    <!-- ***************************************************以上为自动生成代码禁止修改，请在下面添加业务代码************************************************* -->
    <insert id="insertColumn" >
        delete from vmes_column where model_code = 'warehouseInDetail' ;
        insert into vmes_column (
        id,model_code,title_key,title_name,serial_number,
        isdisable,ishide,isedit,ismust,cdate,
        cuser
        ) VALUES
            (replace(uuid(), '-', ''),'warehouseInDetail','parentId','入库单ID',1, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','executeId','执行人ID',2, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','warehouseId','仓库货位ID',3, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','productId','产品ID',4, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','code','货位批次号',5, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','count','入库数量',6, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','state','状态(0:待派单 1:执行中 2:已完成 -1.已取消)',7, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','cuser','创建用户id',8, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','cdate','创建时间',9, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','uuser','修改用户id',10, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','udate','修改时间',11, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','remark','备注',12, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','isdisable','是否启用(0:已禁用 1:启用)',13, '1','1','1','0',now(), 'admin'),
            (replace(uuid(), '-', ''),'warehouseInDetail','id','id',0, '1','1','1','0',now(), 'admin')
    </insert>

    <update id="updateStateByDetail" parameterType="com.yvan.PageData" >
        update vmes_warehouse_in_detail set
        state = #{state},
        udate = now()
        <where>
            <if test="parentId != null and parentId != ''" >
                and parent_id = #{parentId}
            </if>
        </where>
    </update>

    <!-- 列表(全部) 自动创建，禁止修改-->
    <select id="dataList" parameterType="com.yvan.PageData" resultType="com.xy.vmes.entity.WarehouseInDetail">
        select
            id id,
            parent_id parentId,
            execute_id executeId,
            warehouse_id warehouseId,
            product_id productId,

            code code,
            count count,
            state state,
            cuser cuser,
            cdate cdate,

            uuser uuser,
            udate udate,
            remark remark,
            isdisable isdisable
        from vmes_warehouse_in_detail
        <choose>
            <!--
                mapSize (0 or is null) 无查询参数-业务层传入
                isQueryAll 是否查询全部
                fasle: (false or is null) 无查询条件-查询结果集返回空或list.size()==0
                true : 无查询条件-返回全部业务表数据
            -->
            <when test="(mapSize == null || mapSize == 0) and 'true' != isQueryAll ">
                where 1=2
            </when>
            <otherwise>
                <where>
                    <!--isSelfExist 是否考虑自己在业务表中是否存在
                        false: (false or is null) 无需考虑自己在业务表中是否存在
                        true : 需要考虑自己在业务表中是否存在
                    -->
                    <if test="id != null and id != ''" >
                        <choose>
                            <when test="'true' == isSelfExist">
                                <![CDATA[ and id <> #{id} ]]>
                            </when>
                            <otherwise>
                                and id = #{id}
                            </otherwise>
                        </choose>
                    </if>
                    <if test="parentId != null and parentId != ''" >
                        and parent_id = #{parentId}
                    </if>
                    <if test="code != null and code != ''" >
                        and code = #{code}
                    </if>
                    <if test="productId != null and productId != ''" >
                        and product_id = #{productId}
                    </if>
                    <if test="warehouseId != null and warehouseId != ''" >
                        and warehouse_id = #{warehouseId}
                    </if>

                    <!--queryStr 自定义sql查询语句-->
                    <if test="queryStr != null and queryStr != ''" >
                        and ${queryStr}
                    </if>
                </where>
            </otherwise>
        </choose>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>

    <!-- 自动创建，可以修改 -->
    <select id="getDataListPage" parameterType="com.yvan.PageData" resultType="java.util.Map">
        select
            id id,
            parent_id parentId,
            execute_id executeId,
            warehouse_id warehouseId,
            product_id productId,

            code code,
            count count,
            state state,
            cuser cuser,
            cdate cdate,

            uuser uuser,
            udate udate,
            remark remark,
            isdisable isdisable
        from vmes_warehouse_in_detail
        <where>

            <!--queryStr 自定义sql查询语句-->
            <if test="queryStr != null and queryStr != ''" >
                and ${queryStr}
            </if>
        </where>
        <if test="orderStr != null and orderStr != ''" >
            order by ${orderStr}
        </if>
    </select>



</mapper>